{# purpose of this template: images view xml view #}
<?xml version="1.0" encoding="{{ pageGetVar('meta.charset') }}" ?>
<images>
{% for image in items %}
    {{ include('@RKLandingPagesModule/Image/include.xml.twig') }}
{% else %}
    <noImage />
{% endfor %}
</images>
