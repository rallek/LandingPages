{# purpose of this template: pages view xml view #}
<?xml version="1.0" encoding="{{ pageGetVar('meta.charset') }}" ?>
<pages>
{% for page in items %}
    {{ include('@RKLandingPagesModule/Page/include.xml.twig') }}
{% else %}
    <noPage />
{% endfor %}
</pages>
